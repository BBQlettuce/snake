(function () {
  window.SnakeGame = window.SnakeGame || {};


  var Coord = window.SnakeGame.Coord = function(pos) {
    this.x = pos[0];
    this.y = pos[1];
  };

  var plus = Coord.plus = function(){

  };

  var equals = Coord.equals = function(){

  };

  var isOpposite = Coord.isOpposite = function(){

  };

  })();
